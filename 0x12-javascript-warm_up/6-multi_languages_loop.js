#!/usr/bin/node
//rints 3 lines: (like 1-multi_languages.js) but by using an array of string and a loop

const lang = ['C is fun', 'Python is cool', 'JavaScript is amazing'];
for (let index = 0; index < lang.length; index++) {
	cosole.log(lang[index]);
}
